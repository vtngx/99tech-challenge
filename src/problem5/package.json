{
  "name": "resource-api",
  "version": "1.0.0",
  "description": "expressjs + typescript api server",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "dist/app.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only --poll src/app.ts",
    "build": "tsc",
    "start": "node dist/app.js",
    "compose": "docker-compose up --build",
    "compose:watch": "docker-compose up --watch",
    "compose:down": "docker-compose down -v"
  },
  "dependencies": {
    "class-validator": "^0.14.2",
    "express": "^4.19.2",
    "pg": "^8.11.3",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.3.19"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
